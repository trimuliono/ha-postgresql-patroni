kind: Service
apiVersion: v1
metadata:
  name: hasura-chm-tri
  namespace: tri
  labels:
    app: hasura-chm-tri
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
  selector:
    app: hasura-chm-tri
  type: ClusterIP
