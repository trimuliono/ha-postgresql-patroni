kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: hasura-chm-tri
  namespace: tri
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - hasura-chm-tri.alldataint.com
      secretName: wildcard-tls
  rules:
    - host: hasura-chm-tri.alldataint.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hasura-chm-tri
                port:
                  number: 8080
